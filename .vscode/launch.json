{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C++ File",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${fileDirname}",
            "stopAtEntry": false,
            "externalConsole": true,
            "MIMode": "gdb",
            "preLaunchTask": "build active file"
        }
    ]
}
